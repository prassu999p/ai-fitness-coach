// Temporary stub — will be replaced in Amendment E
export function validateProgram(_program: any): { valid: true; errors: []; normalized: any } {
  return { valid: true, errors: [], normalized: _program }
}
